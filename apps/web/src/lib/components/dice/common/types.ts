export type Sparkle = {
  x: number;
  y: number;
  vx: number;
  vy: number;
  life: number;
  size: number;
  hue: number;
};

export type Vec3 = [number, number, number];
export type Vec2 = [number, number];
